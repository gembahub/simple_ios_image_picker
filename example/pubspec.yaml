name: simple_ios_image_picker_example
description: Demonstrates how to use the simple_ios_image_picker plugin.
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.0.5 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  simple_ios_image_picker:
    path: ../

  cupertino_icons: ^1.0.2
  cross_file: ^0.3.3+8

dev_dependencies:
  integration_test:
    sdk: flutter
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
